# @package _global_

habitat_hitl:
  window: ~
  experimental:
    headless:
      do_headless: True
      # reference for debugging
      # debug_video_writer:
      #   num_frames_to_save: 50
      #   filepath_base: "./debug_video"
      # exit_after: 40
  remote_gui_mode: True
