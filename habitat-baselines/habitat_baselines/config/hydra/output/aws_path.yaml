# @package hydra
run:
  dir: /fsx-siro/akshararai/hab3/${hydra.job.name}/${now:%Y-%m-%d}/${now:%H-%M-%S}
  subdir: ${hydra.job.num}
sweep:
  dir: /fsx-siro/akshararai/hab3/${hydra.job.name}/${now:%Y-%m-%d}/${now:%H-%M-%S}
  subdir: ${hydra.job.num}
