# @package hydra
run:
  dir: /data/home/akshararai/data/hab3/${hydra.job.name}/${now:%Y-%m-%d}/${now:%H-%M-%S}
  subdir: ${hydra.job.num}_${hydra.job.override_dirname}
sweep:
  dir: /data/home/akshararai/data/hab3/${hydra.job.name}/${now:%Y-%m-%d}/${now:%H-%M-%S}
  subdir: ${hydra.job.num}_${hydra.job.override_dirname}
